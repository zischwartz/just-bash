/** Append untrusted collections without argument spreading and with a cumulative cap. */
export declare function appendBoundedElements<T>(target: T[], values: readonly T[], maximum: number, site: string): void;
